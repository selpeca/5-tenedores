import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content:{
        position: "absolute",
        top:0,
        right: 0,
        padding: 5,
        backgroundColor:"#fff",
        paddingLeft: 10,
        borderBottomLeftRadius: 20,
    }
});